import React from 'react';

interface BlogEmmaChatProps {
  language: string;
}

// Chat functionality removed — component kept as empty shell to avoid breaking imports
const BlogEmmaChat: React.FC<BlogEmmaChatProps> = () => {
  return null;
};

export default BlogEmmaChat;
